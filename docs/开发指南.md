# 开发指南

## 扩展新API
1. 新增 Controller 接口。
2. 增加 `@RequirePermission` 与 `@OperationLog`。
3. 使用 `BusinessException` 进行业务异常返回。
4. 在 Swagger 注解中补充 `@Tag/@Operation`。

## 扩展插件
- 实现 `TaskProcessorPlugin`。
- 使用 `@Component` 注册后自动被插件注册中心发现。
