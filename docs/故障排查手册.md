# 故障排查手册

## 401 未授权
- 检查 `X-API-Key` 是否传递。
- 检查 client 状态是否为“启用”。

## 403 权限不足
- 检查客户端是否属于对应 RBAC 分组。
- 检查接口权限声明 `@RequirePermission`。

## 数据源测试失败
- 检查 `connectionConfig` 中 `host/port`。
- 检查网络连通性与防火墙。
